[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "tacos"
version = "0.1.0"
description = "A package for tacos"
authors = [{name = "Your Name", email = "your.email@example.com"}]

[tool.setuptools.packages.find]
where = [".."]